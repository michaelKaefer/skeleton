{% trans_default_domain 'registration' %}

{% extends 'base.html.twig' %}

{% block title %}{{ 'title'|trans }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('registration') }}
{% endblock %}

{#Checkbox with link...#}
{% form_theme form _self %}

{% block checkbox_radio_label -%}
    {#- Do not display the label if widget is not defined in order to prevent double label rendering -#}
    {%- if widget is defined -%}
        {% set is_parent_custom = parent_label_class is defined and ('checkbox-custom' in parent_label_class or 'radio-custom' in parent_label_class or 'switch-custom' in parent_label_class) %}
        {% set is_custom = label_attr.class is defined and ('checkbox-custom' in label_attr.class or 'radio-custom' in label_attr.class or 'switch-custom' in label_attr.class) %}
        {%- if is_parent_custom or is_custom -%}
            {%- set label_attr = label_attr|merge({class: (label_attr.class|default('') ~ ' custom-control-label')|trim}) -%}
        {%- else %}
            {%- set label_attr = label_attr|merge({class: (label_attr.class|default('') ~ ' form-check-label')|trim}) -%}
        {%- endif %}
        {%- if not compound -%}
            {% set label_attr = label_attr|merge({'for': id}) %}
        {%- endif -%}
        {%- if required -%}
            {%- set label_attr = label_attr|merge({class: (label_attr.class|default('') ~ ' required')|trim}) -%}
        {%- endif -%}
        {%- if parent_label_class is defined -%}
            {%- set label_attr = label_attr|merge({class: (label_attr.class|default('') ~ ' ' ~ parent_label_class)|replace({'checkbox-inline': '', 'radio-inline': '', 'checkbox-custom': '', 'radio-custom': ''})|trim}) -%}
        {%- endif -%}
        {%- if label is not same as(false) and label is empty -%}
            {%- if label_format is not empty -%}
                {%- set label = label_format|replace({
                    '%name%': name,
                    '%id%': id,
                }) -%}
            {%- else -%}
                {%- set label = name|humanize -%}
            {%- endif -%}
        {%- endif -%}

        {{ widget|raw }}
        <label{% with { attr: label_attr } %}{{ block('attributes') }}{% endwith %}>
            {{- label is not same as(false) ? (translation_domain is same as(false) ? label|raw : label|trans(label_translation_parameters, translation_domain)|raw) -}}
            {{- form_errors(form) -}}
        </label>
    {%- endif -%}
{%- endblock checkbox_radio_label %}
{#Checkbox with link...#}

{% block body %}
    <div class="register card shadow">
        <div class="card-body">
            <h1 class="h5 card-title">
                {{ 'headline'|trans }}
            </h1>

            <p>
                {{ 'go_to_login'|trans({
                    url: path('login', { _locale: app.request.locale }),
                })|raw }}
            </p>

            <div>
                {{ form_start(form) }}
                    <div class="row">
                        <div class="col-md-12">
                            {{ form_row(form.email, {
                                label: 'email_label',
                                attr: {
                                    autofocus: true,
                                    placeholder: 'email_placeholder',
                                }
                            }) }}
                        </div>
                        <div class="col-md-12">
                            {{ form_row(form.plainPassword, {
                                label: 'password_label',
                                attr: {
                                    placeholder: 'password_placeholder',
                                }
                            }) }}
                        </div>
                        <div class="col-md-12">
                            {{ form_row(form.agreeToTermsAndDataPrivacy, {
                                label: 'approve_terms_label'|trans({
                                    '{terms_url}': '#',
                                    '{privacy_url}': '#',
                                }),
                            }) }}
                            {{ form_row(form.receivesNewsletter, {
                                label: 'approve_newsletter_label',
                            }) }}
                        </div>
                        <div class="col-md-12">
                            <button class="btn btn-primary">{{ 'register_button_label'|trans }}</button>
                        </div>
                    </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('registration') }}
{% endblock %}
